Module variable3
    Sub main()
        Dim a, b As Integer
        a = 20
        b = 40
        Dim d As Single = 0.36

        Dim s As String = "SSI"
        Dim x As Boolean = True
        Console.WriteLine("Integer A=" + a.ToString + "b=" + b.ToString)
        Console.WriteLine("String=" + s)
        Console.WriteLine("Boolean=" & x)
        Console.WriteLine("Single =" + d.ToString)
        Console.ReadKey()
    End Sub

End Module
